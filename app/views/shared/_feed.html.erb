<% if @feed_items.any? %>
<ol class="grid sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-2 mt-10  w-full">
<%= render @feed_items %>
</ol>
<%= will_paginate @feed_items,
params: { controller: :static_pages, action: :home } %>

<% end %>